#include <iostream>

int main() {
	// valid variable identifiers include letters, numbers and underscore. The identifier cannot begin with a number.
	int a, m1, cos, _Beta, min_1;
	std::cin >> a >> m1;
	std::cout << a << m1;
	std::cin.get();
	// invalid variable identifiers
	//int x', k"m, x+1, sin x, a1-a2;
}